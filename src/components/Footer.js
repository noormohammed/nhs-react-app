import React from "react";

const Footer = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        width: "100%",
        height: 40,
        backgroundColor: "#333",
        color: "#fff",
        position: "fixed",
        bottom: 0,
      }}
    >
      Footer Menu
    </div>
  );
};

export default Footer;
